# Privacy notes (MVP)

- **Ask:** `save_prompt` and `store_prompt_text` control whether raw prompt text is persisted with the session.
- **Logs:** Avoid logging full user prompts in production.
- **Aggregate theme signals:** Future editorial weighting should use anonymized counts, not raw prompts.

Admin and ops: see deployment runbook when added.
